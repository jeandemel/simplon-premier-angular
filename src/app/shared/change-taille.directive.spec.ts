import { ChangeTailleDirective } from './change-taille.directive';

describe('ChangeTailleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTailleDirective();
    expect(directive).toBeTruthy();
  });
});
